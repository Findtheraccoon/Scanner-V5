"""Checks individuales de la batería del Validator.

Cada módulo expone una función async `run(...) -> TestResult` con la
firma específica que el `runner` invoca. Faltan implementaciones de
A, B, C, E, F, G — el runner los emite como `skip` mientras tanto.
"""

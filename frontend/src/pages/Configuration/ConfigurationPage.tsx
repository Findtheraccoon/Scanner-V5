import { StubPage } from "../_stub/StubPage";

export function ConfigurationPage() {
  return (
    <StubPage
      title="configuración"
      description="4 pasos verticales: config + backup S3 / API keys / fixtures + slot registry / arranque motores con progress bar del Validator."
    />
  );
}

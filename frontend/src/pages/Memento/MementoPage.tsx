import { StubPage } from "../_stub/StubPage";

export function MementoPage() {
  return (
    <StubPage
      title="memento"
      description="Consulta de stats por slot + catálogo de patrones. Modo lectura sobre la base operativa + archivo."
    />
  );
}

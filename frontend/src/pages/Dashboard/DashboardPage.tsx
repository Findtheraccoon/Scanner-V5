import { StubPage } from "../_stub/StubPage";

export function DashboardPage() {
  return (
    <StubPage
      title="dashboard"
      description="Piloto Master + 4 secciones (motores · slots · base de datos · pruebas validación). Refleja en vivo el estado del backend."
    />
  );
}
